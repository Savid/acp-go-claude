---
title: "Metadata"
description: "Every key the adapter reads and writes under the _meta.claude namespace"
---

All Claude-specific public metadata lives under `_meta.claude`. Foreign metadata
namespaces are preserved untouched for their owners. Inside the Claude
namespace, lifecycle requests are strict: a non-object `_meta.claude`, an
unknown key, and a wrong-typed known value are all rejected with an
invalid-params error naming the offending field.

## Initialize capabilities

Initialize advertises capability descriptors under `_meta.claude`:

| Descriptor | Describes |
| --- | --- |
| `fork` | The `_claude/session/fork` extension method. |
| `elicitation` | Session-scoped elicitation support. |
| `rawEvent` | The raw-event opt-in (`enabledBy`, `maxBytes`, `defaultEnabled`). |
| `sessionStore` | Store `format` and key shape. |
| `structuredOutput` | Where the output schema is supplied and the result is emitted. |
| `providerAuth` | Present only while `WithProviderAuthRoot` is usable, bare mode is disabled, and no agent-wide static authentication owns the process. Carries `methods` and `injectionKey: "_meta.claude.options.providerAuth"`. Lifecycle injection accepts one strictly decoded `anthropic` binding with an `api` credential whose sole metadata entry is `env: "CLAUDE_CODE_OAUTH_TOKEN"` or `env: "ANTHROPIC_API_KEY"`. Responses report `_meta.claude.providerAuth.injection` as `applied`, `noop`, or `conflict`. |

Initialize also advertises reserved envelopes separately from the Claude
namespace:

The fourth reserved envelope, `acp-go.dev/lifecycle`, is enabled by an
`initialize` capability containing exact integer `version: 1` rather than advertised
unconditionally, and its placement is the exact inverse of the envelopes below.
When that capability is present and the `Serve` output supports close or write-deadline
interruption, the `acp-go.dev/lifecycle` answer is placed on
`InitializeResponse._meta` — the response's own top-level `_meta` — and never on
`agentCapabilities._meta`, where the reserved envelopes below ride, nor under
`_meta.claude`. A non-interruptible output still serves ordinary ACP traffic but
omits the lifecycle answer. The answer reports the active containment proof
class, `updatesOutsidePrompt: true`, and `activityKinds: []`. No capability or
no interruptible carrier omits the answer. Whole-tree quiescence is available
only when a host authority owns the native tree; ordinary execution advertises
no quiescence proof class.

`activityKinds` is empty because no captured native trace proves the stable
identity and parentage required for a lifecycle activity. Native task frames
still map to ordinary typed tool-call updates, and background work still opens
agent-origin turns; neither is promoted into a fabricated activity registry.

### Work between prompts

Claude's process outlives every prompt it serves, so work that continues after
`session/prompt` returns still reaches the host. Each such excursion is one
fresh agent-origin lifecycle turn: `state_update{state: "running", cause:
"activity"}` naming a turn no `prompt_accepted` introduced, then ordinary
`session/update` content in native order, then `state_update{state: "idle"}`
carrying that turn's outcome. The excursion's durable prefix is committed before
that idle, so the terminal event only ever reports what the store already holds.

Exactly one turn holds the foreground at a time, and an open excursion is not
preempted. Background activity being live refuses nothing, but a prompt that
arrives while an agent-origin turn is actually open is answered `-32600` with
`{"error":"backpressure","limit":"session_foreground"}` before it is dispatched:
it leaves no `prompt_accepted` and writes no frame to the harness, and the
excursion keeps its pending permission or elicitation owned and live. Once the
native terminal settles that excursion, the retried prompt runs as a turn of its
own.

A task keeps the route of the prompt that started it. Every later frame that
names the task — raw frames, typed tool updates, notifications, and their usage —
stays on that route even when another prompt owns the foreground. A result whose
`origin.kind` is `task-notification` names no task: the harness origin carries a
kind alone, so its usage and cost ride the foreground that read it, it retires
no task correlation, and it never ends the prompt or agent-origin excursion
currently reading native traffic. One task may notify any number of times inside
one excursion. Settlement waits for an ordinary native result or system idle.

Permissions and elicitations raised after native initialization but before a
successful `session/new`, `session/load`, `session/resume`, or `session/fork`
response write are retained on a provisional exact route. They reach the host
only after the full response write succeeds and the session's autonomous route
and lifecycle snapshot have been published.

Permissions and elicitations the harness raises after a prompt returns are
owned by the agent-origin turn of the excursion they belong to. A callback whose route names
no live owner — a retired prompt's, or one from an ended incarnation — announces
no action and carries no correlation value, whatever state the stream is in.

Because `updatesOutsidePrompt` is advertised, a between-prompt frame this
adapter has already consumed is never dropped. If its mapping, its ACP
emission, its lifecycle event, or its durable prefix fails, the incarnation that
produced it is contained: pending permissions and elicitations are answered, the
native process is closed behind the reader, no further content is emitted on
that stream, and later `session/prompt` or `session/load` addressed to that same
incarnation is answered `-32603` with
`{"error":"claude_internal_failure","class":"autonomous_stream_failed"}`. A
relaunched process opens a new incarnation and clears the refusal.

```json
{"acp-go.dev/route":{"version":1}}
```

```json
{
  "acp-go.dev/mediaEnvelope": {
    "maxBytes": 6291456,
    "maxPromptBytes": 6291456,
    "maxDimension": 0,
    "imageFormats": ["image/png", "image/jpeg", "image/gif", "image/webp"],
    "documentFormats": ["application/pdf"]
  }
}
```

The media envelope is always advertised. `maxBytes` and `maxPromptBytes` are the
decoded per-image and per-prompt byte gates this adapter actually enforces, so
they move with `WithImageLimits`. They are the *effective* bounds, not the
configured fields: a zero or oversized `MaxInputBytesPerImage` is enforced at the
ACP frame clamp and `maxBytes` reports that clamp, while a zero
`MaxInputBytesPerPrompt` enforces no total and `maxPromptBytes` truthfully
reports `0`. `maxDimension` is `0` because Claude enforces
no per-dimension pixel bound — `0` means "no bound", never "zero". `imageFormats`
is the inbound media-type allowlist and `documentFormats` lists the media types
an embedded blob resource becomes a native document block for. Every value is
an adapter bound; none of them describes what a selected model accepts, which
stays a prompt-time result.

```json
{"acp-go.dev/handoff":{"version":1}}
```

The handoff envelope is advertised **only** when `WithInputHandoffRoot` is set.
Its absence tells a host the option never reached this adapter, and every
handoff-form image block is then rejected `invalid_handoff`. A prompt image
block opts into the form with empty `data`, a `file://` uri under the configured
root, and exactly this block `_meta`:

```json
{
  "acp-go.dev/handoff": {
    "version": 1,
    "digest": "<sha256 hex, 64 lowercase characters>",
    "sizeBytes": 123456
  }
}
```

Exactly those three fields: a missing field, an extra field, and a wrong-typed
field are each rejected, and the `message` says which happened. `sizeBytes` is
load-bearing rather than a hint — it bounds how much the adapter reads, so a file
that turns out to be larger is `handoff_digest_mismatch`. The adapter reads to
that declaration, verifies the size and digest, and then applies the same gates
an embedded image passes. See
[Models and config](/docs/features/models-config).

Every `session/prompt` and every `session/cancel` MUST carry exactly
`_meta["acp-go.dev/route"] = {"version":1,"turnNonce":"..."}`.
Missing, extra, wrong-typed, unsupported-version, and stale values are rejected.
The nonce is fresh and unguessable for each turn and is at most 4096 bytes.

The refusal on `session/prompt` is the uniform `-32602` `{error, field}` object,
and the two verdicts are distinct facts a host acts on differently:

| Value | Answer |
| --- | --- |
| The key is absent | `{"error":"missing","field":"_meta[\"acp-go.dev/route\"]"}` |
| The value is not an object | `{"error":"unsupported","field":"_meta[\"acp-go.dev/route\"]"}` |
| `version` is missing or is not exactly `1` | `{"error":"unsupported","field":"_meta[\"acp-go.dev/route\"].version"}` |
| `turnNonce` is missing, empty, non-string, or over 4096 bytes | `{"error":"unsupported","field":"_meta[\"acp-go.dev/route\"].turnNonce"}` |
| Any other member is present | `{"error":"unsupported","field":"_meta[\"acp-go.dev/route\"].<member>"}` |

`missing` says the host forgot a key it owes and should add it; `unsupported`
says the host sent a value that is present and refused. The two are never
collapsed. Route validation runs before the lifecycle correlation value is read,
so a prompt that gets both wrong reports the route refusal alone.
A cancel is authenticated on every turn state, never only while a turn runs: a
session with no active turn holds no nonce that can authorize one, so its cancel
is refused with no native effect at all — Claude is not interrupted, pending
permission and elicitation requests are untouched, and the native process stays
open.

Every live-turn `session/update` and `_claude/rawEvent` notification carries the
same exact `{version, turnNonce}` object in its notification `_meta`; raw-event
payloads also carry their structural `sessionId`. Lifecycle and load-history
updates are not turn-scoped and therefore do not carry a turn envelope.

Outbound form and URL elicitation requests preserve their native metadata and
add a route object containing `version`, `sessionId`, `turnNonce`, and exactly
one of `toolCallId` or `requestId`. A native request that already owns the
reserved key is rejected rather than overwritten. Permission requests remain
correlated by their structural ACP session and tool-call fields. While lifecycle
version 1 is negotiated, prompts additionally carry submission correlation and
permission and elicitation requests carry action correlation under
`_meta["acp-go.dev/lifecycle"]`. Lifecycle events use only identity-only
`session_info_update` notifications as carriers.

While that key is negotiated, no native work exists that the stream cannot
describe. A native process is announced by its incarnation's opening snapshot
before the adapter starts reading it, and a snapshot the host never received
contains that process instead of leaving it running. A prompt frame is written
only once the stream can announce its acceptance, so a stream that already lost
an event refuses the turn before the harness sees it; if a lifecycle event fails
after the harness has taken the frame, the turn it opened is contained before the
prompt's error returns.

The lifecycle key is refused on `session/new`, `session/load`, `session/resume`,
`session/cancel`, `session/close`, `session/delete`, `session/list`, both
`session/set_config_option` variants, `session/set_mode`, `authenticate`,
`logout`, `_claude/session/fork`, `_claude/rateLimits`, and every
`_claude/auth/*` method. Refusals name `_meta["acp-go.dev/lifecycle"]`, except
on `session/cancel`, where the route is the authenticator and is validated
first: an absent route is refused `missing` on
`_meta["acp-go.dev/route"]`, a malformed one and a nonce that no longer names
the running turn are refused `unsupported` on the member that failed, and only a
cancel whose route names the running turn is refused for the lifecycle key it
carries. Either verdict refuses before interrupting Claude. `session/cancel` is
a notification and answers nothing, so its refusal is wire-silent; the prompt
shapes above are the only ones a host reads for these keys. The route nonce
authenticates admission to the current session turn, while the lifecycle value
supplies submission and action identity.

On `session/prompt`, the lifecycle correlation value splits three ways on one
field path, and a host reads a different fact from each:

| Value | Answer |
| --- | --- |
| The key is absent while the capability is enabled | `{"error":"missing","field":"_meta[\"acp-go.dev/lifecycle\"]"}` |
| The key is present while the capability was not negotiated | `{"error":"unsupported","field":"_meta[\"acp-go.dev/lifecycle\"]"}` |
| The value is malformed — a non-object, a `version` other than `1`, an empty or over-4096-byte identifier, or an unknown member | `{"error":"unsupported","field":"_meta[\"acp-go.dev/lifecycle\"].<member path>"}` |

Every one of these fires before the prompt is dispatched, so a refused prompt
writes no frame to the harness.

## Session options

Session-lifecycle requests accept options and raw-event opt-in:

```json
{
  "_meta": {
    "claude": {
      "options": {
        "model": "sonnet",
        "permissionMode": "default",
        "systemPrompt": "Use concise answers.",
        "bare": true,
        "extraPathDirs": ["/run/session-42/bin"],
        "outputSchema": {"type": "object"}
      },
      "rawEvent": {
        "enabled": true
      }
    }
  }
}
```

`extraPathDirs` prepends absolute directories, in the order given, to the PATH
of that session's Claude process. They precede every inherited entry, so an
executable placed in one shadows the one PATH would otherwise resolve. A
relative or empty entry fails the session-lifecycle request. Raw `PATH` stays
rejected in `env`; this is the supported way to extend the child's search path.
`CLAUDE_CONFIG_DIR`, `HOME`, and the XDG cache, config, data, runtime, and state
root variables are also rejected in session `env`. The host-selected
`WithHome` and, when supplied, `HostAuthority.NativeEnvironment` remain authoritative.
Every one of those names is matched the way the platform names variables: exact
case on Unix, where `PATH` and `path` are two variables, and case-insensitively
on Windows, where they are one.

`outputSchema` must be a non-empty JSON Schema object. An empty object is
rejected rather than read as "no schema", so a request asking for structured
output never silently returns an ordinary turn.

## Model metadata

Session-lifecycle response metadata exposes the selected model under
`_meta.claude.modelId`, its current effort variant under
`_meta.claude.variant`, and the selectable variants under
`_meta.claude.availableVariants`. Individual model options in the session
configuration carry their native facts under
`_meta.claude.supportedEffortLevels`, `_meta.claude.supportsAutoMode`, and
`_meta.claude.contextWindow` when those values are known. See
[Models and config](/docs/features/models-config).

## Update metadata

The terminal main-agent `session/update` notification carries
`_meta.claude.messageId` on its notification envelope. Its value is Claude's
durable transcript-entry UUID for that assistant message, not the Anthropic
API's `msg_*` identifier. The update uses an empty session-info payload so it
does not fabricate visible assistant content. The successful `session/prompt`
response repeats the same field. `session/load` replay restores the field on
the saved assistant update, so a host can use it as the native turn checkpoint
and compare it with restored state. Intermediate `tool_use` assistant messages
and subagent updates do not publish a checkpoint identity.

Session updates derived from a Claude subagent frame carry
`_meta.claude.parentToolUseId`. Its value is the `toolCallId` of the Agent tool
call that spawned the subagent, and the tagged update is that subagent's output
flattened into the ordinary update stream. Message, thought, tool-call, and plan
updates without the key are the main agent's. The tag is set only on live-turn
updates; replayed transcripts do not re-emit it, and subagent frames are excluded
from usage counters.

## Result metadata

Successful prompt responses carry the terminal transcript UUID under
`_meta.claude.messageId`. Structured-output turns emit their result on the
turn's `usage_update` under `_meta.claude.structuredOutput`; the same update
reports token details under `_meta.claude.usage`. Prompt responses do not carry
the structured-output result.
